apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todoapp-pvc
  namespace: todoapp
spec:
  resources:
    requests:
      storage: 512Mi
  accessModes:
    - ReadWriteMany
  volumeName: todoapp-pv
  storageClassName: standard